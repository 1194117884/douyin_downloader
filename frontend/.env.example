# Local development environment — copy this file to .env and modify
# After running wrangler dev to start the worker, set your worker address below
VITE_WORKER_URL=https://your-worker.your-subdomain.workers.dev/api
# Set your API key (must match API_KEY in worker wrangler.toml or wrangler secret)
VITE_API_KEY=your-api-key
