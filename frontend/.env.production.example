# Production environment — copy this file to .env.production
# /api is a relative path (the worker serves the frontend assets, same-origin deployment)
VITE_WORKER_URL=/api
# Set your API key (must match API_KEY in worker wrangler.toml or wrangler secret)
VITE_API_KEY=your-api-key
